@{
    ViewData["Title"] = "Painel PIX";
}
@model IEnumerable<Admin.Models.PagamentoViewModel>

<div class="container-fluid px-0 mt-4">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4 gap-3">
        <div>
            <h1 class="h2 fw-bold text-dark mb-0 d-flex align-items-center gap-2">
                <span class="material-symbols-outlined pix-green" style="font-size: 2.5rem;" translate="no">qr_code_2</span>
                Painel PIX
            </h1>
            <p class="text-muted mb-0 mt-1">Gerenciamento analítico de recebimentos via PIX</p>
        </div>
        <a class="btn btn-pix-green d-flex align-items-center fw-semibold shadow-sm" asp-action="Create">
            <span class="material-symbols-outlined me-2" translate="no">add_card</span>
            Registrar Pagamento
        </a>
    </div>

    <!-- Cards de Resumo -->
    <div class="row g-4 mb-4">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-3 h-100 position-relative overflow-hidden">
                <div class="position-absolute top-0 end-0 p-4 opacity-10">
                    <span class="material-symbols-outlined" style="font-size: 8rem; color: #32BCAD;" translate="no">payments</span>
                </div>
                <div class="card-body p-4">
                    <h2 class="text-uppercase text-muted small fw-semibold mb-2">Total Recebido (PIX)</h2>
                    <div class="d-flex align-items-baseline gap-2 mb-4">
                        @{
                            var totalRecebido = Model.Where(p => p.Status == "PAGO").Sum(p => p.Valor);
                        }
                        <span class="display-4 fw-bold text-dark">R$ @totalRecebido.ToString("N2")</span>
                        <span class="badge bg-success-subtle text-success d-flex align-items-center">
                            <span class="material-symbols-outlined me-1" style="font-size: 0.875rem;" translate="no">trending_up</span>
                            +12%
                        </span>
                    </div>
                    <div class="row border-top pt-4 mt-4">
                        <div class="col-6 col-md-4">
                            <p class="text-muted small mb-1">Transações</p>
                            <p class="h4 fw-bold text-dark mb-0">@Model.Count()</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-3 h-100">
                <div class="card-body p-4">
                    <h3 class="text-uppercase text-muted small fw-semibold mb-3">Status dos Pagamentos</h3>
                    @{
                        var pagosCount = Model.Count(p => p.Status == "PAGO");
                        var pendenteCount = Model.Count(p => p.Status == "PENDENTE");
                        var canceladoCount = Model.Count(p => p.Status == "Cancelado");
                        var totalCount = Model.Count();
                        var pagoPercent = totalCount > 0 ? Math.Round((double)pagosCount / totalCount * 100, 1) : 0;
                        var pendentePercent = totalCount > 0 ? Math.Round((double)pendenteCount / totalCount * 100, 1) : 0;
                        var canceladoPercent = totalCount > 0 ? Math.Round((double)canceladoCount / totalCount * 100, 1) : 0;
                    }
                    <div class="d-flex flex-column gap-3">
                        <div class="d-flex align-items-center">
                            <span class="badge bg-success rounded-circle p-2 me-2"></span>
                            <span class="text-muted small fw-medium" translate="no">Pago (@pagoPercent%)</span>
                        </div>
                        <div class="d-flex align-items-center">
                            <span class="badge bg-warning rounded-circle p-2 me-2"></span>
                            <span class="text-muted small fw-medium" translate="no">Pendente (@pendentePercent%)</span>
                        </div>
                        <div class="d-flex align-items-center">
                            <span class="badge bg-danger rounded-circle p-2 me-2"></span>
                            <span class="text-muted small fw-medium" translate="no">Cancelado (@canceladoPercent%)</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Filtros -->
    <div class="card border-0 shadow-sm rounded-3 mb-4">
        <div class="card-body p-3">
            <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center gap-3">
                <div class="d-flex flex-wrap align-items-center gap-2">
                    <span class="material-symbols-outlined text-muted me-1" translate="no">filter_list</span>
                    <span class="text-muted small fw-medium me-2">Status:</span>
                    <button class="btn btn-sm btn-secondary active">Todos</button>
                    <button class="btn btn-sm btn-outline-warning">Pendente</button>
                    <button class="btn btn-sm btn-outline-success">Pago</button>
                    <button class="btn btn-sm btn-outline-danger">Cancelado</button>
                </div>
                <div class="d-flex align-items-center gap-2 text-muted small">
                    <span>Mostrando últimos 30 dias</span>
                    <button class="btn btn-link btn-sm text-decoration-none p-0 pix-green">Alterar período</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Tabela -->
    <div class="card border-0 shadow-sm rounded-3">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="px-4 py-3 text-uppercase small fw-semibold text-muted">ID Reserva</th>
                            <th class="px-4 py-3 text-uppercase small fw-semibold text-muted">Cliente</th>
                            <th class="px-4 py-3 text-uppercase small fw-semibold text-muted">Valor</th>
                            <th class="px-4 py-3 text-uppercase small fw-semibold text-muted">Status</th>
                            <th class="px-4 py-3 text-uppercase small fw-semibold text-muted">Data</th>
                            <th class="px-4 py-3 text-uppercase small fw-semibold text-muted">Nº Transação</th>
                            <th class="px-4 py-3 text-uppercase small fw-semibold text-muted text-end">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            @foreach (var pagamento in Model)
                            {
                                <tr class="pagamento-row" data-status="@pagamento.Status">
                                    <td class="px-4 py-3 small text-muted font-monospace">@pagamento.ReservaId</td>
                                    <td class="px-4 py-3 small fw-medium">
                                        @(pagamento.ClienteNome ?? "-")
                                    </td>
                                    <td class="px-4 py-3 small fw-bold">R$ @pagamento.Valor.ToString("N2")</td>
                                    <td class="px-4 py-3">
                                        @if (pagamento.Status == "PAGO")
                                        {
                                            <span class="badge bg-success-subtle text-success border border-success-subtle">Pago</span>
                                        }
                                        else if (pagamento.Status == "PENDENTE")
                                        {
                                            <span class="badge bg-warning-subtle text-warning border border-warning-subtle">Pendente</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger-subtle text-danger border border-danger-subtle">Cancelado</span>
                                        }
                                    </td>
                                    <td class="px-4 py-3 small text-muted">
                                        @(pagamento.DataPagamento?.ToString("dd/MM/yyyy HH:mm") ?? "-")
                                    </td>
                                    <td class="px-4 py-3 small text-muted font-monospace">
                                        @(string.IsNullOrEmpty(pagamento.QrCode) ? pagamento.Id.ToString() : pagamento.QrCode)
                                    </td>
                                    <td class="px-4 py-3 text-end">
                                        <div class="btn-group dropstart">
                                            <button class="btn btn-sm btn-link text-muted" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                                <span class="material-symbols-outlined" translate="no">more_vert</span>
                                            </button>
                                            <ul class="dropdown-menu">
                                                <li><a class="dropdown-item" asp-action="Details" asp-route-id="@pagamento.Id">Ver Detalhes do Pagamento</a></li>
                                                <li><a class="dropdown-item" asp-action="Edit" asp-route-id="@pagamento.Id">Editar Pagamento</a></li>
                                                <li><hr class="dropdown-divider"></li>
                                                @if (pagamento.Status != "PAGO")
                                                {
                                                    <li><a class="dropdown-item" asp-action="ConfirmarPagamento" asp-route-id="@pagamento.Id">Confirmar Pagamento</a></li>
                                                }
                                                else
                                                {
                                                    <li><a class="dropdown-item" asp-action="CancelarReserva" asp-route-id="@pagamento.Id">Cancelar Reserva</a></li>
                                                }
                                                <li><a class="dropdown-item" asp-action="Details" asp-route-id="@pagamento.Id">Visualizar Número de Transação</a></li>
                                                <li><hr class="dropdown-divider"></li>
                                                <li><a class="dropdown-item text-danger" asp-action="Delete" asp-route-id="@pagamento.Id">Excluir</a></li>
                                            </ul>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="7" class="text-center py-4 text-muted">
                                    Nenhum pagamento encontrado.
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Paginação igual as outras views -->
    @{
        var paginaAtual = ViewBag.PaginaAtual is int ? (int)ViewBag.PaginaAtual : 1;
        var tamanhoPagina = ViewBag.TamanhoPagina is int ? (int)ViewBag.TamanhoPagina : 8;
        var totalPaginas = ViewBag.TotalPaginas is int ? (int)ViewBag.TotalPaginas : 1;
        if (totalPaginas < 1) { totalPaginas = 1; }
        if (paginaAtual < 1) { paginaAtual = 1; }
        if (paginaAtual > totalPaginas) { paginaAtual = totalPaginas; }
        var paginaAnterior = paginaAtual > 1 ? paginaAtual - 1 : 1;
        var proximaPagina = paginaAtual < totalPaginas ? paginaAtual + 1 : totalPaginas;
        var totalItens = ViewBag.TotalItens is int ? (int)ViewBag.TotalItens : (Model?.Count() ?? 0);
        var inicio = totalItens == 0 ? 0 : ((paginaAtual - 1) * tamanhoPagina) + 1;
        var fim = totalItens == 0 ? 0 : inicio + (Model?.Count() ?? 0) - 1;
    }
    
    <div class="d-flex justify-content-between align-items-center mt-4">
        <span class="text-muted small">Mostrando @inicio a @fim de @totalItens resultados</span>
        <nav aria-label="Navegação de página">
            <ul class="pagination mb-0">
                <li class="page-item @(paginaAtual == 1 ? "disabled" : "")">
                    <a class="page-link"
                       asp-action="Index"
                       asp-route-pagina="@paginaAnterior"
                       asp-route-tamanhoPagina="@tamanhoPagina"
                       aria-label="Anterior">
                        <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>
                @for (var i = 1; i <= totalPaginas; i++)
                {
                    <li class="page-item @(i == paginaAtual ? "active" : "")">
                        <a class="page-link"
                           asp-action="Index"
                           asp-route-pagina="@i"
                           asp-route-tamanhoPagina="@tamanhoPagina">@i</a>
                    </li>
                }
                <li class="page-item @(paginaAtual == totalPaginas ? "disabled" : "")">
                    <a class="page-link"
                       asp-action="Index"
                       asp-route-pagina="@proximaPagina"
                       asp-route-tamanhoPagina="@tamanhoPagina"
                       aria-label="Próximo">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            </ul>
        </nav>
    </div>
</div>
